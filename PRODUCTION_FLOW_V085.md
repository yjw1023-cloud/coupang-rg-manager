# 생산/BOM 재고 흐름 — v0.8.5

2026-08-07 사용자 확정 규칙.

## 생산 처리
- 생산 버튼 1회로 BOM 구성품 차감과 완제품 RG 입고를 하나의 DB 트랜잭션에서 처리한다.
- BOM 구성품 재고는 항상 `자체창고`에서 확인하고 차감한다.
- 필요한 구성품 중 하나라도 자체창고 재고가 부족하면 생산 전체를 취소하며 부분 차감하지 않는다.
- 완제품은 생산수량만큼 `쿠팡RG` 창고에 즉시 입고한다.
- 완제품을 자체창고에 먼저 넣었다가 별도 창고이동하는 단계는 사용하지 않는다.
- 생산이력(`production_orders.warehouse_id`)은 완제품 입고 목적지인 `쿠팡RG`로 기록한다.
- 구성품 재고거래 유형은 `생산소모`, 완제품 재고거래 유형은 `생산RG입고`로 기록한다.
- 생산원가는 BOM의 `구성품별 qty_per × 구성품 unit_cost` 합계로 계산하고 완제품 `unit_cost`를 갱신한다.
- self-BOM(완제품이 자기 자신을 구성품으로 포함)은 생산을 차단한다.

## 기존 UI 호환
기존 생산 화면이 `warehouse_id`를 전달하는 구조는 유지하지만 v0.8.5부터 실제 재고 경로에는 사용하지 않는다. 실제 경로는 무조건 `자체창고 구성품 차감 -> 쿠팡RG 완제품 입고`다.

## 검증
격리 테스트 DB에서 BOM `원재료 2개 -> 완제품 1개`, 생산수량 3개로 테스트:
- 자체창고 원재료 10 -> 4
- 자체창고 완제품 0 -> 0
- 쿠팡RG 완제품 0 -> 3
- 생산이력 warehouse_id = 쿠팡RG
- 생산원가 = 원재료 단가 100 × 2 = 200원/완제품
