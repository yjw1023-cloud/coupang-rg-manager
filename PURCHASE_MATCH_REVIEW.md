# 매입상품 매칭 검토 UI — v0.9.0

## 사용자 요구
매입 Excel 업로드 후 자동매칭된 품목이 접힌 개별 카드 안에 숨겨져 있어 전체 매칭이 맞는지 한눈에 확인하기 어렵다. 따라서 Excel의 매입상품과 새 ERP의 자체창고 상품을 한 표에서 나란히 비교하고, 잘못된 매칭은 그 표에서 직접 수정할 수 있어야 한다.

## v0.9.0 동작
- 기존 자동매칭 알고리즘과 매입확정 로직은 변경하지 않는다.
- 기존 로직이 선택한 매칭상품을 읽어 `상품 매칭 확인` 영역에 전체 비교표로 노출한다.
- 표 컬럼: `No. / 매입상품 / 수량 / 매입원가 / 매칭상품 / 상태`.
- `매입상품`은 Excel F열 상품명과 G열 상세를 함께 표시한다.
- 수량은 AB열, 매입원가는 W열을 사용한다.
- `매칭상품`은 자체창고 상품만 선택 가능하며 상품명, 품목코드, 자체창고 현재고를 함께 표시한다.
- 자동매칭이면 `자동 100%` 같은 상태를 표시하고, 사용자가 표에서 바꾸면 `수동 변경`으로 표시한다.
- 표에서 수정한 선택은 기존 숨겨진 매칭 selectbox의 동일한 widget key에 반영되므로 기존 매입확정 로직이 수정된 상품을 그대로 사용한다.
- 기존 개별 매칭 expander는 호환성을 위해 내부적으로 계속 실행하지만 비교표가 준비된 후 화면에서는 숨긴다.
- 첫 업로드 시 기존 매칭 widget key/options를 한 번 수집한 뒤 Streamlit rerun으로 비교표를 즉시 표시한다.

## 기존 원칙 유지
- 매입 매칭 후보는 자체창고 상품만 허용한다.
- 쿠팡RG-only 상품은 매입 후보에서 제외한다.
- W열 = 원화 매입원가, AB열 = 매입/입고 수량.
- 매입 입고는 자체창고로 처리한다.
- v0.8.9의 매입 차수 입력 및 기존 9~15차 복구 로직과 함께 동작한다.
