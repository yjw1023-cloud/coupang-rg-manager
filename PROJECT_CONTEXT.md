# Coupang Rocket Growth Manager — Project Context

이 문서는 새 ChatGPT 세션에서도 프로젝트의 핵심 맥락을 빠르게 복구하기 위한 기준 문서다. 코드 수정 전에 반드시 이 문서와 최신 커밋, update/latest.json을 먼저 확인한다.

## 사용자 목표
- 쿠팡 로켓그로스 판매를 관리하는 Windows 로컬 프로그램
- 단순 재고관리보다 **상품별 손익관리 정확성**이 최우선
- 혼자 운영하므로 화면은 빠르게 이해되고 클릭 수가 적어야 함
- 매일 보는 잠정손익과 월 정산 후 확정손익을 분리 관리

## 기술 구조
- Windows PC 로컬 실행
- Streamlit UI
- SQLite DB: `data/rocketgrowth.db`
- 실행: `run.bat`
- 자동 업데이트 서버: GitHub 저장소 `yjw1023-cloud/coupang-rg-manager`
- 업데이트 manifest: `update/latest.json`
- 업데이트 시 사용자 데이터(`data`, `.venv`, `sample_data`)는 보호

## 핵심 데이터 원칙
1. 모든 쿠팡 자료 연결의 기본키는 가능한 경우 `옵션ID`
2. 재고현황 판매통계는 판매금액을 신뢰하지 않고 **판매수량/취소수량용**으로 사용
3. 판매수수료 정산자료는 실제 판매가, 판매자 쿠폰, 수수료, VAT, 정산금 확인용
4. 실현매출은 등록 판매가가 아니라 판매자 부담 쿠폰 등을 반영한 실제 정산 기준 금액으로 계산
5. 입출고/배송비는 고정값으로 박지 않고 실제 정산 이력과 프로모션 할인 후 최종비용을 사용
6. 신규상품 45일 면제 등으로 실제 물류비가 0원이면 예상비용도 최근 실제값을 우선 사용
7. 반품은 매출 차감 후 바로 정상재고로 돌리지 않고 **반품창고**로 이동
8. 광고비는 광고 성과보고서의 `광고집행 옵션ID` 기준으로 상품별 귀속
9. 월 확정 광고비는 광고 정산서의 `청구가능 광고비` 기준으로 보정
10. 예상값은 삭제하지 않고 확정값, 차이, 오차율을 남김

## 손익 구조
### 일일 예상손익
- 판매수량: 재고현황 판매통계
- 예상 실현판매가: 가장 최근 확정 실현가 + 필요 시 현재 쿠폰 수동 보정
- 원가: 자체 상품/원가 DB
- 판매수수료: 최근 실제 수수료율/수수료 패턴
- RG 비용: 최근 실제 입출고비/배송비
- 광고비: 일별 또는 조회기간 광고 성과보고서
- 반품/기타비용 반영
- 광고 포함 이익과 광고 제외 이익을 둘 다 표시

### 월 확정손익
- 판매수수료 정산
- 입출고배송비 정산
- 반품회수비 정산
- 광고 월 정산서
- 자체 원가 DB
- 프로그램 계산과 쿠팡 실제 정산 합계를 대조

## 쿠팡 파일 종류
- `재고현황.xlsx`: 실시간 판매수량 및 재고 파악용. 파일 자체에 조회기간이 없을 수 있으므로 사용자가 조회 시작일/종료일을 지정해야 함.
- `판매수수료.xlsx`: 실제 판매가/판매자쿠폰/수수료/정산용
- `입출고배송비.xlsx`: 실제 입출고비/배송비/프로모션 할인 반영
- `반품회수비.xlsx`: 반품회수비/반품재입고비
- `cmg_settlement_detail_*.xlsx`: 월 광고 정산서. `청구가능 광고비`를 확정비용으로 사용
- 광고 성과보고서: `광고집행 옵션ID`를 상품별 광고비 귀속키로 사용

## 현재 확인된 샘플/검증 메모
- 2026년 7월 자료를 기준으로 프로그램 검증 진행
- 입출고비 합계: 970,575원
- 배송비 합계: 1,576,761원
- 반품회수비: 46,613원
- 반품재입고비: 12,100원
- 광고 청구가능액: 3,597,175원
- 최근 샘플 실현가 예: 에어방석 7,800원, 소가죽 벨트 5,900원
- 벨트 최근 입출고비/배송비가 프로모션으로 0원인 사례 확인

## UI 원칙
- 개발자용 내부 컬럼명(`data_type`, `period_start`, `None`, 내부 id 등)을 사용자 화면에 노출하지 않음
- 금액: `3,597,175원`
- 수량: `1,498건`
- 비율: `21.4%`
- 기본 정렬은 최신순/중요도순
- 대시보드 우선 정보: 매출, 예상이익, 광고비, 이익률, 적자상품, 재고부족
- 색상: 네이비/파랑 기본, 정상 초록, 주의 주황, 적자/오류 빨강
- 업로드 자료는 상태 카드로 표시
- 표보다 한눈에 보는 KPI/상태를 우선

## 재고/BOM 원칙 (v0.8.8 기준)
- 창고: 자체창고 / 쿠팡RG / 반품창고 / 불량·폐기
- **마이너스 재고를 허용한다.** 실제로 발생한 판매·생산을 재고 부족 때문에 막지 않는다. 부족분은 음수 재고로 남겨 누락 입고/생산/조정이 있음을 드러내는 신호로 사용한다.
- 생산 흐름: `자체창고 BOM 구성품 차감 -> 완제품 쿠팡RG 즉시 입고`.
- 생산 시 구성품 재고가 부족해도 BOM 필요수량 전체를 자체창고에서 차감한다. 예: 현재 3개, 필요 4개면 생산 후 -1개.
- 생산원가 계산 및 생산이력 유지. self-BOM(완제품=구성품)은 데이터 오류이므로 계속 차단한다.
- `재고현황 판매통계` 신규 업로드 시 옵션별 **순 판매수량(net_qty)** 만큼 쿠팡RG 재고를 자동 차감한다. 예: RG 4개, 순판매 5개면 -1개.
- 동일 판매통계 파일 재업로드는 중복으로 판단하여 재고를 두 번 차감하지 않는다.
- 동일 기간의 다른 판매통계 파일로 교체할 때는 사용자 교체확인 후 새 자료/새 재고차감을 먼저 생성하고, 기존 기간 판매통계와 기존 `판매차감` 재고원장을 제거한다.
- 과거에 이미 입력된 판매통계를 업데이트 시점에 소급해서 자동 차감하지 않는다. v0.8.8 이후 새로 입력/교체되는 판매통계부터 재고차감 원장을 만든다.
- 원가 변경은 과거 원가를 덮어쓰지 않고 이력으로 남기는 방향

## 매입 Excel 원칙 (v0.8, 2026-08-06 확정)
- **매입상품은 자체창고 관리상품하고만 매칭한다.** 쿠팡RG에만 있는 판매상품/옵션은 매입 매칭 후보가 아니다.
- 업무 흐름은 `매입 -> 자체창고 -> 생산/BOM -> 쿠팡RG` 순서다. 매입 단계에서 RG 판매상품과 직접 연결하지 않는다.
- 매입 Excel의 **W열 = 1개당 매입원가(KRW)** 로 고정한다.
- 매입 Excel의 **AB열 = 매입수량/기본 입고수량** 으로 고정한다.
- 매입금액 = `W열 원가 × AB열 수량`.
- 환율 입력을 사용하지 않고 W열 원가를 원화 기준으로 저장한다.
- 입고창고는 매입 화면에서 사용자가 고르는 방식이 아니라 `자체창고`로 고정한다.
- 자체창고 상품 판정은 원재료/기초상품 또는 자체창고 재고이력이 있는 상품을 기준으로 하며, RG-only 상품은 제외한다.
- 기존 ERP 이관 기능의 상품 매칭은 별도 로직으로 유지하여 매입 화면의 자체창고 필터가 이관 기능에 영향을 주지 않게 한다.
- 2026-07-30 실제 매입 파일 검증: 상품행 8개, W×AB 기준 매입금액 합계 1,346,790원.
- 상세 규칙은 `PURCHASE_IMPORT.md`를 참조한다.

## 재고현황 판매통계 날짜 처리 (v0.8.7 기준)
- 파일 안에 조회기간이 없을 수 있으므로 프로그램이 파일만 보고 날짜를 추정하면 안 됨.
- 기본 조회기간은 **가장 최근에 완료된 월요일~일요일**. 예: 2026-08-07 금요일에는 2026-07-27(월)~2026-08-02(일)이 기본값.
- 시작일/종료일은 언제든 직접 수정 가능하며 7일로 강제하지 않는다. 월초/월말에는 `7/27~7/31`, `8/1~8/2`처럼 분할 입력 가능.
- 동일 기간 자료가 이미 있으면 교체 여부를 명시적으로 확인한다.

## 기존 Claude ERP 이관 (v0.7)
- 메뉴: `📥 기존ERP 이관`
- 기존 ERP 기본 경로는 `C:\ERP\erp.db`; WAL 모드이므로 이관 전 기존 ERP 프로그램 종료 권장
- 실제 제공된 ERP DB 검증 결과: 품목 199개(완제품 103 / 원재료 96), 매입 528건, BOM 41건, 생산 97건, 재고 233행, 판매 737건, 광고 13건
- 완제품 103개 중 99개는 숫자형 쿠팡 옵션ID 형태이며 다수는 현재 RG Manager 상품과 옵션ID로 정확 연결 가능
- 샘플 이관 매칭 결과 199개 중 197개 자동 처리, 2개 직접 확인 필요
- 원재료 JDS 코드는 기존 내부코드를 유지해 기초상품으로 생성
- 매입 528건은 역사적 원가층으로 이관하고 중복방지 링크를 남김
- BOM 41건 및 생산 97건은 이력으로 이관
- 자체창고와 반품창고 현재고는 기본 이관. 쿠팡RG 현재고는 쿠팡 재고현황이 더 신뢰도가 높으므로 기본적으로 이관하지 않으며 사용자가 명시적으로 선택할 때만 이관
- 현재고는 단순 가산하지 않고 기존 ERP 스냅샷 수량에 맞추는 재고조정(delta) 방식
- 기존 ERP 판매 737건, 광고 13건, 과거 actual_pnl은 새 쿠팡 원정산 자료와 중복될 수 있으므로 이관하지 않음
- 기존 RG 상품의 현재 unit_cost가 0일 때만 기존 ERP 최신 매입단가로 보충하는 옵션 제공
- 같은 ERP 스냅샷과 같은 매입/생산 ID는 재이관해도 중복 생성하지 않음
- 이관 모듈 파일: `erp_import_v07.py`; v0.7 앱은 raw GitHub 파일을 SHA-256 검증 후 사용

## 업데이트 운영 규칙
새 버전을 배포할 때:
1. GitHub 코드 수정
2. `VERSION.txt` 버전 증가
3. `update/latest.json`의 version/message/files 갱신
4. 사용자 DB 및 데이터 폴더는 절대 배포 파일로 덮어쓰지 않음
5. 가능하면 변경 후 기존 계산 검증 테스트 실행
6. v0.7 이후 새 Python 모듈은 별도 `.py` 파일 방식 우선. 대형 코드를 `app.py` 안에 직접 임베드하지 않는다.

## 다음 세션에서 권장 시작 절차
1. 이 `PROJECT_CONTEXT.md`를 읽는다.
2. `update/latest.json`을 읽는다.
3. 최신 `app.py`, `VERSION.txt` 및 필요한 별도 모듈을 확인한다.
4. 사용자가 요청한 수정사항을 기존 손익 원칙을 깨지 않도록 구현한다.
5. 수정 후 버전을 올리고 GitHub에 배포한다.

## 절대 피할 것
- 등록 판매가만으로 매출 계산
- RG 비용을 고정 단가로 가정
- 반품을 즉시 정상재고로 복구
- 광고비를 캠페인 전체로만 잡고 상품별 귀속을 무시
- 월 정산자료와 일일 경영용 예상손익을 하나의 숫자로 혼합
- 업데이트 과정에서 `data/rocketgrowth.db`를 덮어쓰기
- 매입상품을 쿠팡RG 판매상품에 직접 연결하거나 RG 창고로 직접 매입입고
- 매입 Excel에서 W열 이외 값을 매입원가로, AB열 이외 값을 기본 수량으로 사용하는 것
- 생산 구성품 재고가 부족하다는 이유로 실제 생산 기록을 막는 것
- 판매통계는 저장하면서 쿠팡RG 재고차감을 누락하는 것
- 같은 판매통계 파일 업로드로 재고를 중복 차감하는 것
- 대형 Python 모듈을 zlib/base64 문자열로 app.py 안에 직접 임베드
