# Legacy ERP Import Audit — 2026-08-07

원본 Claude ERP `erp.db`와 현재 RG Manager `rocketgrowth.db`를 1:1 비교한 결과.

## 원본 구조
- 품목 199개
- JDS 코드 100개 / 숫자형 쿠팡 옵션ID 99개
- raw 96개 / finished 103개
- 매입 528건 / BOM 41건 / 생산 97건 / 재고 233행
- 원본 DB 참조 무결성은 정상

## 확인된 이관 오류
1. `erp_import_v07.py`에서 pandas Series의 `r.name`을 상품명 컬럼으로 잘못 사용했다.
   - `r.name`은 행 인덱스이므로 신규 생성된 상품 140개의 이름이 숫자로 저장됨.
   - create_raw 96개 + create_option 44개 = 총 140개.
   - 예: JDS634c 셀룰로오스 행주 -> 상품명 `147`, JDS634d -> `148`.
2. `legacy_v07_mappings.legacy_item_name` 199개가 모두 행 인덱스 숫자로 저장됨.
3. `purchase_aliases` 199개도 숫자형 source_name/source_key로 생성되어 잘못된 별칭 데이터가 됨.
4. JDS 코드와 숫자형 쿠팡 옵션ID의 역할 구분을 item_type만으로 처리해 4개 JDS finished 품목이 RG 상품에 잘못 합쳐짐.
   - JDS0477 점착식 나뭇잎 메모지 -> 94103975794
   - JDS0408 탁구공 수집기 -> 94185578349
   - JDS730 공 CD -> 95251561252
   - JDS0159 부직포 신발 주머니 -> 94475374150(A4 문서 주머니) — 명백한 오매칭
5. 이 합침으로 BOM 41개 중 3개가 자기 자신을 구성품으로 참조하는 self-BOM이 됨.
   - 94103975794 -> 자기 자신 3개
   - 94185578349 -> 자기 자신 1개
   - 95251561252 -> 자기 자신 50개
6. 위 4개 JDS 품목의 과거 매입 4건과 자체창고 재고도 잘못 합쳐진 RG product_id에 연결됨.
   - JDS0159: 자체창고 1,575 / 매입 1,975개 @ 24원
   - JDS0408: 자체창고 -24 / 매입 76개 @ 4,700원
   - JDS0477: 자체창고 80 / 매입 260개 @ 401원
   - JDS730: 자체창고 600 / 매입 3,000개 @ 174원

## 정상 이관된 부분
- 매입 528건은 날짜/수량/단가/총액 자체는 원본과 일치함.
- 생산 97건은 날짜/수량/연결 자체는 원본과 일치함.
- 자체창고/반품창고 현재고 스냅샷은 현재 매핑 기준으로 수량 자체는 일치함.
- 쿠팡RG 현재고는 기존 설계대로 원본 ERP에서 기본 이관하지 않음.

## 수정 원칙
- JDS 코드 = 자체창고/내부 품목으로 취급. `item_type`이 finished여도 RG 상품과 자동 이름매칭하지 않는다.
- 숫자형 코드 = 쿠팡 옵션ID 상품 후보.
- 신규 생성 상품명은 반드시 원본 `item_name` 컬럼을 사용한다. pandas `Series.name` 사용 금지.
- 140개 숫자 상품명 복구.
- legacy_v07_mappings 199개 source name 복구.
- 잘못된 purchase_aliases 199개 정리/재생성.
- 잘못 합쳐진 4개 JDS 품목은 독립 product로 복원하고 매입/재고/BOM 연결을 재배치.
- 기존 정상 RG 상품의 마케팅 상품명은 원본 Claude ERP 이름으로 덮어쓰지 않는다.

## 상태
- 2026-08-07: 감사 완료. 아직 사용자 실제 DB에 복구 패치는 적용하지 않음.
