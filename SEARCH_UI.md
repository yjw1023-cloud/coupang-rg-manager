# 공통 검색 UI — v0.9.6

## 목적
ERP의 상품/품목 목록이 길어질수록 브라우저 `Ctrl+F`에 의존하지 않고 ERP 화면 안에서 바로 상품을 찾을 수 있게 한다.

## 적용 원칙
- 상품/품목 중심의 읽기 전용 표에는 공통 검색창을 자동 표시한다.
- 주요 적용 대상: 재고관리, 상품·원가, 품목관리 목록, 반품관리, 매입이력의 목록/전체내역, 판매·손익의 상품별 표 등.
- 앞으로 새로 추가되는 목록형 표도 상품명/품목코드/옵션ID 계열 컬럼이 있으면 자동으로 검색 대상이 된다.
- 업로드/확정처럼 행 편집 결과가 업무처리를 일으키는 `data_editor`는 안전을 위해 공통 필터를 강제로 적용하지 않는다.

## 검색 범위
검색어는 표시 중인 표의 모든 컬럼을 대상으로 부분일치 검색한다.
따라서 다음 값으로 찾을 수 있다.
- 상품명
- 품목명
- 품목코드 / 상품코드
- 쿠팡 옵션ID
- 표에 표시되는 날짜, 상태 등 기타 값

공백으로 여러 단어를 입력하면 모든 단어가 포함되는 행만 남긴다.

## 화면 동작
- 검색어가 없으면 전체 목록 표시.
- 입력 즉시 목록 필터링.
- 검색 중에는 `검색 결과 n개 / 전체 n개` 표시.
- 검색어를 지우면 즉시 전체 목록으로 복귀.
- 재고관리는 검색 후 동일 필터가 전체/자체창고/쿠팡RG/반품창고 탭에 공통 적용된다.

## 매입이력 예외
`purchase_history_v094.py`의 상품별 매입이력은 행 클릭 선택과 인덱스 연결을 유지하기 위해 기존 전용 `상품 검색`을 그대로 사용한다.
공통 검색 모듈은 해당 클릭 목록에는 두 번째 검색창을 만들지 않는다.
전체 매입내역 표에는 공통 검색이 적용된다.

## 데이터 안전
- 검색은 화면 표시만 필터링한다.
- DB, 재고, 원가, BOM, 생산, 매입, 반품 데이터는 수정하지 않는다.

## 관련 코드
- `search_ui_v096.py`
- `app.py`
- `VERSION.txt` = `0.9.6`
