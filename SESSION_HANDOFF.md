# ERP SESSION HANDOFF — latest working context

이 문서는 새 ChatGPT 세션에서 `yjw1023-cloud/coupang-rg-manager` 작업을 즉시 이어가기 위한 인수인계 기준 문서다.

## 새 세션 시작 시 반드시 확인
1. `PROJECT_CONTEXT.md`
2. `SESSION_HANDOFF.md` (이 문서)
3. `VERSION.txt`
4. `update/latest.json`
5. 최신 커밋과 해당 버전의 patch module

현재 기준 버전: **v0.9.4**

---

## 프로젝트 기본 구조
- Windows 로컬 ERP
- Streamlit UI
- SQLite DB: `data/rocketgrowth.db`
- 실행: `run.bat`
- 자동 업데이트: GitHub `yjw1023-cloud/coupang-rg-manager`
- 업데이트 후에는 ERP/검은 명령창까지 완전히 종료 후 재실행하는 것을 기본으로 한다.
- 사용자 데이터 폴더는 업데이트로 덮어쓰지 않는다.

## 사용자 운영 원칙
- 혼자 운영하므로 화면은 한눈에 확인되고 클릭 수가 적어야 한다.
- 정확한 재고/원가/손익 연결이 UI 장식보다 우선한다.
- 쿠팡 자료 연결은 가능한 경우 옵션ID를 기본키로 사용한다.
- 개발자용 내부 ID나 불필요한 시스템 컬럼은 화면에 노출하지 않는다.

---

# 1. 기존 Claude ERP 이관 및 복구

사용자가 제공한 원본 Claude ERP `erp.db` 기준 검증 수치:
- 품목 199개 (완제품 103 / 원재료 96)
- 매입 528건
- BOM 41건
- 생산 97건
- 재고 233행
- 판매 737건
- 광고 13건

기존 이관 코드의 주요 오류와 복구:
- pandas `r.name`을 상품명으로 잘못 써 숫자형 상품명이 다수 생성됨 → 정상 상품명 복구.
- 숫자형 잘못된 alias/mapping 복구.
- 자체창고 JDS 품목 4개가 RG 상품과 잘못 합쳐졌던 문제 복구.
- 그 영향으로 생긴 self-BOM 3건 복구.
- DB 복구는 `legacy_repair_v082.py` 계열에서 1회성/중복안전 방식으로 처리.
- 원본 Claude ERP DB 자체가 망가졌던 것이 아니라, 이전 이관 코드가 잘못됐던 것.

---

# 2. 창고/재고 원칙

창고 구분:
- 자체창고
- 쿠팡RG
- 반품창고
- 불량·폐기(내부 원칙상 존재 가능)

재고 화면:
- 전체 / 자체창고 / 쿠팡RG / 반품창고 탭 방식.
- 개별 창고 탭에서는 해당 창고 재고만 본다.
- 쿠팡 RG 상품 내부 `item_code`의 `CP-<옵션ID>`는 사용자 화면에서는 `CP-`를 숨긴다.

마이너스 재고:
- **허용한다.**
- 실제 판매/생산을 재고 부족 때문에 막지 않는다.
- 부족분을 음수 재고로 남겨 누락 입고/생산/조정 확인 신호로 사용한다.

---

# 3. BOM / 생산 원칙

- BOM과 재고는 동일한 `product_id` 기준으로 연결.
- self-BOM(완제품=구성품)은 데이터 오류이므로 생산 차단.
- 생산 흐름은 `자체창고 BOM 구성품 차감 -> 완제품 쿠팡RG 즉시 입고`.
- 구성품 재고가 부족해도 생산은 진행하며 부족분은 마이너스 재고.
- BOM 필요수량 × 생산수량만큼 전체 차감.
- 생산원가는 BOM 구성품 원가 합계 기준.
- 관련 패치: `production_v085.py`, `inventory_flow_v088.py`.

---

# 4. 판매통계 / 재고 차감 원칙

`재고현황 판매통계` Excel:
- 판매금액이 아니라 판매수량/취소수량 및 재고 흐름용.
- 신규 업로드 시 옵션별 순판매수량 `net_qty` 만큼 쿠팡RG 재고 차감.
- RG 재고가 부족해도 판매수량 전체 차감해 음수 허용.
- 동일 파일 재업로드는 재고를 두 번 차감하지 않는다.
- 동일 기간 다른 파일 교체 시 기존 판매차감을 제거하고 새 파일 기준 재반영.
- v0.8.8 이전 과거 자료는 업데이트 시점에 자동 소급차감하지 않는다.

조회기간 UI:
- 기본값은 가장 최근에 완료된 월요일~일요일.
- 시작일/종료일 자유 수정, 7일 강제하지 않음.
- 동일 기간 자료가 있으면 교체 확인.
- 관련 패치: `sales_period_v087.py`, `inventory_flow_v088.py`.

---

# 5. 매입 Excel 원칙

업무 흐름:
- `매입 -> 자체창고 -> 생산/BOM -> 쿠팡RG`

매칭 대상:
- **매입상품은 자체창고 관리상품과만 매칭한다.**
- RG-only 상품은 매입 후보에서 제외.

Excel 열:
- **W열 = 1개당 매입원가(KRW)**
- **AB열 = 매입수량 / 기본 입고수량**
- 매입금액 = `W × AB`
- 환율/RMB 재계산하지 않음.
- 매입 입고창고는 자체창고 고정.

2026-07-30 실제 파일 검증:
- 상품행 8개
- W×AB 합계 매입금액 1,346,790원

관련 문서/패치:
- `PURCHASE_IMPORT.md`
- `purchase_v08.py`

---

# 6. 매입 차수 관리

기존 Claude ERP `purchases.note`의 차수 기록을 복구함.
확인된 차수:
- 9차 16행
- 10차 14행
- 11차 7행
- 12차 3행
- 13차 17행
- 14차 2행
- 15차 12행
- 총 71행

현재 매입 Excel 업로드 화면:
- `매입 차수` 입력칸 존재.
- `16` 입력 → `16차` 정규화.
- 같은 Excel의 모든 확정 매입행에 동일 차수 적용.
- `purchase_lines.purchase_batch`와 import notes에 기록.
- 관련 문서/패치: `PURCHASE_BATCH.md`, `purchase_batch_v089.py`.

---

# 7. 품목관리

메뉴: `📋 품목관리`

기능:
- 품목목록
- 신규등록
- 기존 품목 검색/수정
- 상품명 수정
- 기준원가 수정
- 사용/중지 상태

등록 구분:
- 자체창고 품목: 품목코드 + 상품명 + 원가
- 쿠팡RG 상품: 상품명 + 쿠팡 옵션ID + 원가

품목코드/옵션ID는 재고/BOM/정산 연결키이므로 단순 수정 허용하지 않는다.
삭제보다 사용중지 방식.
관련 패치: `item_ui_v086.py`.

---

# 8. 매입상품 매칭 검토 UI — v0.9.1

표 구조:
- `No.`
- `매입상품`
- `품목코드`
- `상품명`
- `현재재고`
- `매입수량`
- `매입원가`
- `상태`
- `매칭 수정`

중요 UI 요구:
- 품목코드 / 상품명 / 현재재고를 각각 별도 칸으로 표시.
- 순서: 품목코드 → 상품명 → 현재재고.
- 맨 오른쪽 `매칭 수정`에서 다른 자체창고 상품 선택 가능.
- 수정 후 새 상품 기준 매칭값을 사용.

관련 코드:
- `purchase_match_ui_v090.py`
- `purchase_match_ui_v091.py`

---

# 9. 매입이력 메뉴 — 최신 v0.9.4

사용자 요구:
- 아이템별로 과거에 **언제, 몇 개를, 얼마에 샀는지** 필요할 때마다 즉시 보고 싶음.
- 상품을 드롭다운에서 고르는 방식이 아니라 **매입상품이 화면에 쭉 나열되고, 상품을 클릭하면 과거 이력이 바로 아래 표시**되어야 함.
- 재고는 현재고 확인 역할을 유지하고, 과거 매입 분석은 별도 메뉴로 분리.

메뉴:
- `🗂️ 매입이력`

데이터 연결:
- 별도 이력 DB를 만들지 않음.
- `purchase_lines.product_id = products.id` 기준으로 기존 매입행을 그대로 조회.
- 기존 Claude ERP에서 이관된 과거 매입과 앞으로 매입 Excel로 등록되는 신규 매입이 동일 상품 이력에 연속 표시.

상품별 매입이력 화면 v0.9.4:
- 상품 선택 드롭다운 제거.
- 매입이력이 있는 상품을 표로 모두 나열.
- 목록 컬럼: 품목코드 / 상품명 / 최근매입일 / 최근매입가 / 누적매입수량 / 누적매입액 / 매입횟수.
- 사용자가 상품 행을 클릭하면 같은 화면 아래쪽에 상세 이력 표시.
- 검색창은 선택용이 아니라 목록 필터 용도.
- Streamlit 행 선택 기능이 없는 구버전에서는 목록을 유지하면서 `선택` 체크 칸 방식으로 동작.

선택 상품 상세:
- 최근 매입가
- 누적 매입수량
- 누적 매입액
- 최근 매입일
- 총 매입 건수 / 최근 차수
- 핵심 상세표: `매입일 / 차수 / 매입수량 / 개당 매입가 / 매입금액`
- 매입이력 2건 이상이면 날짜별 매입단가 추이 차트.
- 원본 파일/시트/원본행과 매입자료 상품명/상세정보는 접힌 영역에서 확인.

전체 매입내역 탭:
- 전체 매입 건수
- 매입상품 수
- 누적 매입액
- 차수 필터
- 전체 최신순 매입표

금액 원칙:
- 개당 매입가: `landed_unit_cost_krw` 우선, 없으면 총매입금액/수량, 마지막으로 `unit_price`.
- 매입금액: `landed_total_krw` 우선, 없으면 `total_amount`.
- 수량: `qty_receipt` 우선, 없으면 `qty_source`.

관련 코드/문서:
- `purchase_history_v092.py`
- `purchase_history_v094.py`
- `PURCHASE_HISTORY.md`

---

# 10. 반품관리 메뉴 — v0.9.3

사용자 요구:
- 현재 반품된 상품 수량, 누적 반품수, 상품별 반품률을 한눈에 보고 싶음.
- 재고 화면에 섞지 않고 별도 분석 메뉴로 관리.

새 메뉴:
- `↩️ 반품관리`

조회기간:
- 전체
- 이번 달
- 최근 30일
- 최근 90일

핵심 지표:
- 반품창고 현재수량
- 반품 보유상품 수
- 누적 반품수
- 반품률
- 반품비용(반품회수비 + 반품재입고비)

데이터 정확성 원칙:
- 반품창고 현재수량은 `inventory_txns` 실제 현재고 합계만 사용.
- 누적 반품수/반품률은 `sales_stats`에 명시적 반품수량/취소수량 또는 판매수량-순판매수량 계산에 필요한 컬럼이 있을 때만 계산.
- 명시적 반품수량이 있으면 `반품수량`으로 표시.
- 취소수량만 있으면 순수 반품과 주문취소를 분리할 수 없으므로 `취소·반품수량` 기준임을 경고.
- 판매통계에 필요한 컬럼이 없으면 누적 반품수/반품률을 추정하지 않고 `계산 불가` 표시.
- **반품회수비 정산자료의 행 개수를 반품건수로 사용하지 않는다.**
- 반품비용은 월 확정손익의 `return_pickup + return_restock` 사용.

상품별 표:
- 품목코드
- 상품명
- 판매수량
- 반품수량 또는 취소·반품수량
- 반품률
- 현재 반품창고
- 반품비용
- 기본 정렬: 반품률 높은 순 → 반품수량 많은 순.

상품별 상세:
- 판매수량 / 반품수량 / 반품률 / 현재 반품창고
- 기간정보가 있는 판매통계는 월별 반품률 추이 차트 표시.

데이터 변경:
- 조회 전용 화면이며 새 테이블 생성/기존 데이터 수정 없음.
- 기존 매입/생산/판매 재고 흐름에 영향 없음.

관련 코드/문서:
- `return_management_v093.py`
- `RETURN_MANAGEMENT.md`

---

# 11. 업데이트/배포 원칙

- `VERSION.txt`와 `update/latest.json`을 최신 버전과 일치시킨다.
- 새 patch module을 manifest의 files 목록에 포함한다.
- 업데이트 후 ERP는 완전 종료 후 재실행.
- 기존 데이터 DB는 업데이트 파일로 덮어쓰지 않는다.
- 복구/마이그레이션 코드는 중복 실행에 안전해야 한다.

현재 버전: **0.9.4**

---

# 12. 새 ChatGPT 세션에서 작업하는 방법

사용자가 “ERP 계속 개발하자”라고 하면:
1. GitHub `yjw1023-cloud/coupang-rg-manager` 연결 확인.
2. `PROJECT_CONTEXT.md`와 `SESSION_HANDOFF.md` 읽기.
3. `VERSION.txt`, `update/latest.json` 확인.
4. 최신 관련 patch module 코드 확인.
5. 사용자의 현재 화면/요구와 최신 버전 코드가 일치하는지 확인 후 수정.
6. 중요한 결정/버그원인/새 업무규칙은 GitHub 문서에 기록.
