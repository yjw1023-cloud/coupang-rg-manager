# ERP SESSION HANDOFF — latest

저장소: `yjw1023-cloud/coupang-rg-manager`
브랜치: `main`
현재 배포 버전: **v0.9.94**

새 세션 시작 시 `PROJECT_CONTEXT.md`, 이 문서, `SESSION_LOG_2026-08-13_PART2.md`, `VERSION.txt`, `update/latest.json`을 순서대로 확인한다.

## 2026-08-13~14 완료사항

- v0.9.80: `🎯 목표·실적관리` 메뉴 미표시 해결. 사용자 화면에서 메뉴 표시 확인.
- v0.9.81~0.9.83: 목표·실적관리 화면을 사용자 엑셀처럼 `합계 + 아이템별 목표/잠정실적/확정실적` 구조로 변경. 아이템 3행 셀 병합, 아이템 열 확대, 숫자 열 축소, 헤더/셀 가운데 정렬 및 색상 적용. 사용자 확인 완료.
- v0.9.84: 목표 엑셀 양식 다운로드 → 엑셀 수기 작성 → 업로드 → 옵션ID 자동매칭 → 선택 월 목표 저장 기능 추가.
- v0.9.85: 목표·실적관리 상단에 매출자료/광고자료가 당월 며칠까지 반영됐는지 표시. 잠정 광고비는 현재 광고성과보고서를 직접 다시 읽어 반영.
- v0.9.86: 판매통계 파일명 날짜 자동인식. 사용자 정상 작동 확인.
- v0.9.87~0.9.91: 광고성과보고서 파일명 날짜 자동인식과 legacy/canonical 광고자료 동기화 수정.
- 과거 잘못 저장된 `A00577001_pa_total_campaign_20260812_20260812.xlsx`의 기간 `8/3~8/9`를 v0.9.91에서 `8/12~8/12`로 DB 교정하도록 수정.
- 사용자 화면에서 광고성과보고서 카드 날짜가 `2026.08.12 ~ 2026.08.12`로 수정된 것을 확인.
- 카드의 `292,674원`은 8/12 광고성과보고서의 광고비 합계, `20행`은 반영 집계 행 수.
- v0.9.92: 목표·실적관리 아이템별 기본 정렬을 **목표수량 내림차순**으로 변경. 같은 수량이면 상품명/옵션ID 순.
- v0.9.93: 목표가 없거나 당월 실적이 없는 활성 완제품도 목표·실적표에 항상 표시하도록 수정. 사이드미러 보조거울/색소폰 넥스트랩 누락 원인 대응.
- v0.9.94: `⚙️ 목표관리 상품 설정` 추가. ERP 품목/재고/매출/BOM/과거 목표이력을 삭제하지 않고 상품을 목표관리에서 제외/복원할 수 있음. 제외 상품은 목표·실적표 목록과 합계, 월말검증, 목표 엑셀 양식에서 제외되며 과거 목표이력은 보존.
- v0.9.94: 목표 엑셀 양식 상품 순서를 목표·실적표와 동일하게 **목표수량 내림차순 → 상품명 → 옵션ID**로 통일. 기존 엑셀 모듈 캐시가 업데이트 직후 남지 않도록 재로딩 처리.

## 다음 세션에서 먼저 확인

1. 현재 로컬 버전이 v0.9.94인지.
2. 목표·실적관리에서 사이드미러 보조거울과 색소폰 넥스트랩이 표시되는지.
3. `⚙️ 목표관리 상품 설정`에서 상품 제외 → 목록/합계/목표 엑셀에서 제거 → 복원까지 정상인지.
4. 새로 내려받은 목표 엑셀의 상품 순서가 목표·실적표와 동일한지.
5. 목표·실적관리 상단 광고비자료와 대시보드 광고비 Excel이 `8월 12일까지`로 유지되는지.

## VAT 관리회계 논의 — 미구현

사용자는 관리회계에서 VAT를 단순화해 `VAT 포함 매출 × 약 5%`를 예상부가세로 차감하는 방식을 고려 중.
후보 계산: `관리이익 = 기존 이익 - 예상부가세`.
아직 구현 지시는 없었으므로 코드 변경하지 않음.

## 핵심 파일

`goal_scope_v0994.py`, `goal_data_status_v0985.py`, `goal_excel_view_v0981.py`, `goal_excel_view_v0983.py`, `goal_excel_upload_v0984.py`, `sales_period_v087.py`, `ad_period_v0987.py`, `data_management_sync_v0988.py`, `provisional_ad_report_v0956.py`, `sales_pnl_zero_v0910.py`, `pnl_month_default_v0915.py`, `app.py`.

## 작업 방식

- 존댓말.
- 복잡한 구현 설명은 길게 하지 않는다.
- 수정 요청이면 GitHub에 직접 반영하고 버전/결과 중심으로 답한다.
- 실제 화면에서 확인하지 않은 것은 확인됐다고 말하지 않는다.

## 다음 채팅 시작 문구

`쿠팡 RG ERP 계속 개발하자. GitHub의 PROJECT_CONTEXT.md, SESSION_HANDOFF.md, SESSION_LOG_2026-08-13_PART2.md 읽고 현재 v0.9.94 상태와 마지막 확인사항부터 이어서 작업해.`
