# ERP SESSION HANDOFF — latest

저장소: `yjw1023-cloud/coupang-rg-manager`
브랜치: `main`
현재 배포 버전: **v0.9.174**

## 새 세션 시작 시 반드시 확인

1. `PROJECT_CONTEXT.md`
2. `SESSION_HANDOFF.md`
3. **`SESSION_LOG_2026-09-05.md`** — 이번 매입 신규품목/BOM 문제의 상세 기록
4. `VERSION.txt`
5. `update/latest.json`
6. 최근 커밋

사용자가 새 채팅에서 이어서 ERP를 개발할 예정이므로, 이미 확인한 내용을 처음부터 다시 묻지 말고 기록과 실제 DB/코드를 기준으로 이어간다.

## 최신 목표·실적관리 변경 — v0.9.174

- `목표·실적관리 > 목표 입력`에서 목표 엑셀 양식을 다운로드할 때 **선택한 목표월의 직전 달 실제 실적**을 기본값으로 자동 채운다.
- 선택한 목표월에 이미 저장된 목표가 있는 상품은 **저장된 목표값을 우선 유지**한다. 전월 실적으로 덮어쓰지 않는다.
- 목표가 아직 없는 상품만 전월 실적으로 채운다.
- 전월 **확정실적**이 있는 상품은 확정실적을 우선 사용한다.
- 확정실적이 없는 상품은 목표·실적표에서 사용하는 최종 **잠정실적 계산 경로**를 사용한다. 즉 판매자료뿐 아니라 현재 광고성과보고서, 반품 통합, 수동조정값까지 반영한 잠정실적을 사용한다.
- 전월에 실적 자체가 없는 신규/미판매 상품은 입력칸을 빈칸으로 남긴다.
- 엑셀 다운로드 자체는 DB를 수정하지 않는다. 사용자가 엑셀 값을 수정한 뒤 업로드하고 `목표 저장`을 눌렀을 때만 목표 DB에 반영한다.
- 핵심 파일: `goal_prev_actual_template_v09174.py`, `goal_excel_upload_v0984.py`, `update/latest.json`.

## v0.9.173 목표입력 안내문

- `목표·실적관리 > 목표 입력` 탭의 목표 엑셀 다운로드 영역 위에 **판매 종료 상품 정리 안내**를 항상 표시한다.
- 완전히 판매 종료한 상품은 `품목관리 > 품목 삭제/보관`에서 여러 상품을 체크해 보관처리하도록 안내한다.
- ERP상 재고가 남아 있지만 실제 재고가 0인 옛 상품은 `재고 0 정리 후 선택 품목 보관`으로 처리할 수 있다.
- 상품은 ERP에서 활성 상태로 유지하고 목표관리에서만 빼려면 `목표·실적관리 > 목표관리 상품 설정 > 목표관리에서 제외`를 사용한다.
- 목표관리 제외 상품은 목표·실적표와 목표 엑셀 양식에서만 빠지고 매출·재고·정산 이력에는 영향을 주지 않는다.
- `app.py`에서 `goal_excel_upload_v0984`를 Streamlit rerun 시 캐시 제거 대상으로 추가해 업데이트 직후 최신 목표입력 코드를 다시 읽게 했다.

## 현재 매입/BOM 복구 상태 — v0.9.172

- 실제 ERP DB는 구형 `erp.db`가 아니라 **`data/rocketgrowth.db`**.
- 사용자가 올린 실제 `rocketgrowth.db` 복사본을 직접 감사한 결과를 기준으로 v0.9.172를 작성함.
- 대상 매입은 import id **32**, 파일 `18차수입원가.xlsx`.
- 30개 상품/옵션 그룹 중 신규 의도 28개, 실제 정상 신규등록은 22개였고 **6개가 기존 품목에 잘못 매칭**되어 있었음.
- 누락/오매칭 6개: 고무장갑 S/M/L, 바늘세트, 치즈보관케이스, 계란보관함.
- v0.9.172는 이 6개를 신규 raw 품목으로 복구하고 해당 import의 `purchase_lines`와 `매입입고` 재고원장만 올바른 product_id로 재귀속한다.
- 기존 잘못 매칭된 품목의 덮인 원가도 과거 정상값으로 복구한다.
- import 32 전체 source_name/source_detail → product_id 매핑을 backfill한다.

## 고무장갑 확정 복구값

- `JDS787` → `고무장갑 [正品南洋 大码 / S]` / 240개 / 약 847원
- `JDS788` → `고무장갑 [正品南洋 大码 / M]` / 250개 / 약 844원
- `JDS789` → `고무장갑 [正品南洋 大码 / L]` / 983개 / 약 849원

완제품/BOM:
- 옵션ID `96012086788` S → `JDS787 × 5`
- 옵션ID `96012086789` M → `JDS788 × 5`
- 옵션ID `96012086790` L → `JDS789 × 5`

기타 복구 신규품목:
- `JDS790` 바늘세트
- `JDS791` 치즈보관케이스 `[整理盒带盖防尘]`
- `JDS792` 계란보관함 `[透明款（opp袋装）]`

## 중요한 검증값

v0.9.172 복구 전후 유지되어야 함:
- import 32 총 매입수량: **6,114개**
- import 32 총 매입금액: **3,579,159원**
- 전체 자체창고 수량 합계: 동일

BOM은 잘못된 3건을 올바른 3건으로 교체하는 것이므로 **전체 56개가 정상**.

## 다음 채팅에서 먼저 확인할 것

사용자가 v0.9.174 적용 후 목표 엑셀을 보여주면:

1. 사이드바 버전이 `0.9.174`인지 확인.
2. 예: 2026-09 목표양식이면 2026-08 실적이 상품별 기본값으로 들어갔는지 확인.
3. 이미 2026-09 목표가 저장돼 있던 상품은 그 목표값이 유지되는지 확인.
4. 전월 확정실적이 있는 상품은 목표·실적표의 확정실적과 엑셀 기본값이 일치하는지 확인.
5. 확정실적이 없는 상품은 목표·실적표의 최종 잠정실적과 일치하는지 확인.
6. 필요하면 최신 `rocketgrowth.db`를 받아 실제 상품별 숫자를 직접 대조한다.

매입/BOM 검증이 아직 필요하면:
- 품목관리 `장갑` 검색에서 raw `JDS787/788/789` 확인.
- BOM `장갑` 검색에서 S→JDS787×5, M→JDS788×5, L→JDS789×5 확인.
- `바늘세트`, `치즈보관케이스`, `계란보관함` JDS790~792 확인.

## 반복하면 안 되는 실수

- JDS/product_id를 화면만 보고 추측해서 BOM에 연결하지 않는다.
- 신규품목이 실제 `products`에 정상 생성됐는지, 매입/재고가 그 product_id에 귀속됐는지 확인한 뒤 BOM을 처리한다.
- 목표 엑셀의 전월 기본값은 **전월 목표**가 아니라 **전월 실제 실적**이어야 한다.
- 사용자가 이미 저장한 현재월 목표를 전월 실적으로 자동 덮어쓰지 않는다.

## 매입 신규품목 현재 규칙

- 현재 구매 엑셀: C=상품명, D=옵션/상세.
- 숫자 컬럼은 사용자 지시대로 **W=원가, AB=수량** 유지. 임의 변경 금지.
- 기본 이름이면 `상품명 [옵션]`으로 저장.
- 사용자가 직접 이름을 바꿨으면 덮어쓰지 않음.
- 매칭 selectbox 최상단에 `＋ 신규상품 등록`.
- 신규 raw는 `active=1`, `option_id IS NULL`, `item_type='raw'`.
- 향후 신규 JDS는 **max(existing numeric JDS)+1**, 빈 번호 재사용 금지.

## 현재 핵심 파일

v0.9.174 목표 엑셀:
- `goal_prev_actual_template_v09174.py`
- `goal_excel_upload_v0984.py`
- `goal_data_status_v0985.py`
- `goal_excel_view_v0981.py`
- `app.py`
- `VERSION.txt`
- `update/latest.json`

v0.9.172 매입/BOM:
- `purchase_import32_repair_v09172.py`
- `purchase_code_generation_v09172.py`
- `rubber_glove_seed_v09161.py`
- `purchase_new_item_persist_v09136.py`
- `purchase_match_ui_v091.py`

상세 매입 문제의 버전별 시행착오/DB 감사 내용은 **`SESSION_LOG_2026-09-05.md`**를 읽는다.
