# ERP SESSION HANDOFF — latest

저장소: `yjw1023-cloud/coupang-rg-manager`
브랜치: `main`
현재 배포 버전: **v0.9.105**

새 세션 시작 시 아래 순서로 확인한다.
1. `PROJECT_CONTEXT.md`
2. `SESSION_HANDOFF.md`
3. `SESSION_LOG_2026-08-14_PART3.md`
4. `VERSION.txt`
5. `update/latest.json`

## 최근 완료사항 요약

- v0.9.93: 목표가 없거나 당월 실적이 없는 활성 완제품도 목표·실적표에 항상 표시.
- v0.9.94: `⚙️ 목표관리 상품 설정` 추가. 상품 삭제 없이 목표관리 제외/복원. 제외 상품은 목표표/합계/월말검증/목표 Excel에서 제외하며 과거이력 보존.
- v0.9.94: 목표 Excel 상품 순서를 목표·실적표와 동일한 `목표수량 내림차순 → 상품명 → 옵션ID`로 통일.
- v0.9.95: 쿠팡 반품 재판매 옵션ID를 시스템 숨김 처리. 내부 매칭/정산에는 유지하되 사용자 상품 화면에는 노출하지 않음.
- v0.9.96: 목표·실적관리 표 열 순서를 `아이템 → 구분 → 매출 → 단가 → 수량 ...`으로 변경.
- v0.9.97: 잠정손익의 수동 예상단가/비용 조정값이 목표·실적관리 잠정실적에도 동일하게 반영되도록 수정.
- v0.9.98: 잠정손익 핵심 열 순서를 `옵션ID → 상품명 → 판매수량 → 취소수량 → 순판매수량 → 예상 실현단가 → 예상매출 → 광고비 → 광고제외이익 → 예상이익 → 이익률(%)`로 변경. 상품명 열 폭/글자 축소.
- v0.9.99~v0.9.101: 목표입력 Excel에 G `수수료단가`, I `입출고배송비단가`, M `상품원가단가` 보조열 추가. 최종적으로 수식이 아니라 ERP가 미리 계산한 반올림 숫자값만 기록.
- v0.9.102: 정상상품 사이드미러 보조거울 `95834379201`, 색소폰 넥스트랩 `95251380743`의 잘못된 시스템 숨김/목표관리 제외를 해제하고 정상 원상품으로 보호.
- v0.9.103: 광고성과보고서 최초 업로드가 내부 동기화 충돌로 중복 경고되던 문제 수정. 같은 날짜/같은 파일은 1건으로 정리.
- v0.9.104: 판매통계 반영 후 방금 저장한 동일 파일을 자기 자신과 중복이라고 경고하던 UI 수정. 동일 파일은 `이미 정상 반영됨`으로 표시하며 재고 이중차감 방지 유지.
- v0.9.105: 자동 예상실현단가를 최근 주문 1건에서 **누적 총 실현매출 ÷ 누적 총 판매수량** 가중평균으로 변경. 단, 수동 예상단가가 있으면 수동값이 무조건 우선. 기존 잠정손익 스냅샷도 새 규칙으로 한 번 자동 재계산.

상세 내용은 `SESSION_LOG_2026-08-14_PART3.md`에 기록.

## 확정된 반품 옵션ID 원칙

- 쿠팡 반품 재판매용 별도 옵션ID는 사용자가 보는 일반 상품이 아니다.
- 사용자에게 노출되는 판매상품은 `품목관리`에 등록된 정상상품만을 원칙으로 한다.
- 반품 옵션ID가 매출/정산/반품매칭에 필요하면 DB 내부에는 유지한다.
- 정상 옵션ID와 다른 반품 재판매 옵션ID의 판매는 원상품에 매칭하여 정상 원상품 매출/수량/손익에 합산한다.
- 시스템 숨김과 사용자가 직접 삭제한 상품 상태는 구분한다.

## 확정된 잠정손익 예상실현단가 기준

1. `manual_expected_sale` 수동 예상단가가 있으면 무조건 우선.
2. 수동값이 없으면 해당 상품의 주문 정산 누적 기준:
   `총 실현매출 ÷ 총 양수 판매수량`
3. 최근 주문 1건 단가는 사용하지 않는다.

관련 파일:
- `realized_sale_unit_avg_v09105.py`
- `pnl_snapshot_refresh_v0966.py`
- `core.py`

## 확정된 잠정손익 원가/개 기준

사용자가 원하는 원칙과 현재 코드가 일치함.

1. 매입/생산 및 재고 입고이력으로 계산한 **이동가중평균 원가** 우선.
2. 재고 이벤트 원가정보가 부족하면 매입이력 + 생산이력 수량가중 평균원가.
3. 그것도 없으면 품목에 수동 입력한 `products.unit_cost` 사용.

즉:
**매입·생산 실제원가 → 없으면 수동입력 원가**

잠정손익 `매출원가`는 최종 `원가/개 × 판매수량` 기준.
관련 파일: `pnl_cost_commission_v0911.py`.

## 목표 Excel 원칙

- 상품 순서는 목표·실적표와 동일.
- 정상 품목만 사용자에게 표시.
- 목표관리 제외 상품은 Excel에서도 제외.
- G열 `수수료단가`, I열 `입출고배송비단가`, M열 `상품원가단가`는 작성 편의용 보조값.
- 세 단가열은 ERP가 다운로드 시 계산해 반올림 숫자로 기록하며 수식은 없음.
- 세 보조열은 목표 업로드 저장값으로 사용하지 않음.

## 다음 세션에서 먼저 확인

1. 로컬 ERP 버전이 v0.9.105인지.
2. 보조거울 `95834379201` 예상실현단가가 기존 비정상 3,192원에서 누적 가중평균 기준으로 정상 변경됐는지.
3. 수동 예상단가 입력 상품은 가중평균보다 수동값이 계속 우선하는지.
4. 잠정손익 `원가/개`가 샘플 상품에서 실제 매입/생산 이동평균 원가와 맞는지 검산.
5. 목표 Excel에 사이드미러 보조거울 `95834379201`, 색소폰 넥스트랩 `95251380743`이 정상 표시되는지.
6. 반품 재판매 옵션ID가 목표관리/품목관리/사용자 상품 선택 화면에 다시 노출되지 않는지.
7. 광고성과보고서 및 판매통계 최초 업로드에서 잘못된 중복 경고가 재발하지 않는지.

## 핵심 파일

`realized_sale_unit_avg_v09105.py`, `pnl_snapshot_refresh_v0966.py`, `pnl_cost_commission_v0911.py`, `product_visibility_v0995.py`, `canonical_visible_products_v09102.py`, `goal_scope_v0994.py`, `goal_data_status_v0985.py`, `goal_excel_view_v0981.py`, `goal_excel_upload_v0984.py`, `goal_excel_format_v09100.py`, `provisional_manual_adjust_v0952.py`, `provisional_ad_report_v0956.py`, `ad_upload_unify_v09103.py`, `sales_period_samefile_v09104.py`, `return_sale_match_v0944.py`, `return_sale_pnl_v0965.py`, `pnl_month_v0961.py`, `pnl_month_v0967.py`, `app.py`.

## 작업 방식

- 존댓말.
- 복잡한 구현 설명은 길게 하지 않는다.
- 수정 요청이면 GitHub에 직접 반영하고 버전/결과 중심으로 답한다.
- 실제 화면에서 확인하지 않은 것은 확인됐다고 말하지 않는다.
- 사용자 데이터 `data`, `.venv`, `sample_data` 덮어쓰기 금지.
- 판매/정산/반품/광고 과거이력은 가능한 물리 삭제보다 보존·매칭·숨김을 우선한다.

## 다음 채팅 시작 문구

`쿠팡 RG ERP 계속 개발하자. GitHub의 PROJECT_CONTEXT.md, SESSION_HANDOFF.md, SESSION_LOG_2026-08-14_PART3.md 읽고 현재 v0.9.105 상태부터 이어서 작업해.`
