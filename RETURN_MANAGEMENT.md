# 반품관리 — v0.9.3

## 목적
상품별로 현재 반품창고에 몇 개가 있는지, 누적 반품/취소수량과 반품률이 어느 정도인지, 반품 관련 비용이 얼마인지 한 화면에서 확인한다.

## 메뉴
- `↩️ 반품관리`
- 조회기간: 전체 / 이번 달 / 최근 30일 / 최근 90일

## 지표 원칙
1. **반품창고 현재수량**은 `inventory_txns`와 `warehouses`의 실제 현재고 합계만 사용한다.
2. **누적 반품수 / 반품률**은 `sales_stats`에 명시적인 반품수량/취소수량 컬럼 또는 `판매수량 - 순판매수량` 계산에 필요한 컬럼이 있을 때만 계산한다.
3. 반품회수비 정산자료의 행 개수를 고객 반품건수로 간주하지 않는다.
4. 명시적 `return_qty` 계열 컬럼이 있으면 `반품수량`으로 표시한다.
5. `cancel_qty` 계열만 있으면 정확한 고객 반품과 주문취소를 분리할 수 없으므로 `취소·반품수량` 기준임을 화면에 경고한다.
6. 판매통계에 필요한 컬럼이 없으면 누적 반품수와 반품률은 `계산 불가`로 표시하고 숫자를 추정하지 않는다.
7. **반품비용**은 월 확정손익의 `return_pickup + return_restock` 합계를 사용한다.

## 상품별 표
- 품목코드
- 상품명
- 판매수량
- 반품수량 또는 취소·반품수량
- 반품률
- 현재 반품창고
- 반품비용

기본 정렬은 반품률 높은 순, 다음으로 반품수량 많은 순이다.

## 상품별 상세
- 판매수량
- 반품수량
- 반품률
- 현재 반품창고
- 판매통계에 기간정보가 있으면 월별 반품률 추이 차트

## 데이터 안전
- 새 테이블을 만들거나 기존 데이터를 수정하지 않는 조회 전용 화면이다.
- 기존 재고/판매/정산 데이터 구조를 동적으로 확인해 사용할 수 있는 신뢰도 높은 컬럼만 사용한다.
- 기존 ERP 이관 및 매입/생산/판매 재고 흐름에는 영향을 주지 않는다.

## 관련 코드
- `return_management_v093.py`
