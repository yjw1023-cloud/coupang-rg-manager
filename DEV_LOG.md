# ERP Development Log

## 2026-08-07 — v0.8.4
- 재고 화면을 창고별 탭으로 분리: `전체 / 자체창고 / 쿠팡RG / 반품창고`.
- 개별 창고 탭은 해당 창고의 현재고가 0이 아닌 품목만 `품목코드 / 상품명 / 현재고`로 표시.
- 과거 이관 코드가 숫자형 쿠팡 옵션ID 상품의 내부 `item_code`에 `CP-` 접두사를 붙였음.
- `CP-`는 내부 구분용 접두사이며 실제 쿠팡 옵션ID는 별도 `option_id`에 순수 숫자로 저장됨.
- v0.8.4부터 사용자 재고 화면에서는 `CP-`를 숨기고 숫자형 옵션ID만 표시.
- DB의 기존 `item_code` 자체는 참조 안정성을 위해 이번 버전에서 변경하지 않음.
- 구현 모듈: `inventory_ui_v084.py`.
