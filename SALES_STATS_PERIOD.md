# 재고현황 판매통계 조회기간 규칙 — v0.8.7

## 기본 동작
- 자료 종류에서 `재고현황 판매통계`를 선택했을 때만 적용한다.
- 조회 시작일/종료일 기본값은 **가장 최근에 완료된 월요일~일요일**이다.
- 예: 2026-08-07(금)에 화면을 열면 기본값은 2026-07-27(월) ~ 2026-08-02(일).

## 날짜 수정
- 기본값은 제안값일 뿐이며 7일로 강제하지 않는다.
- 월초/월말에는 사용자가 원하는 범위로 직접 수정할 수 있다.
- 예: 2026-07-27 ~ 2026-07-31, 2026-08-01 ~ 2026-08-02처럼 나누어 업로드 가능하다.
- 시작일이 종료일보다 늦으면 업로드를 막는다.

## 동일기간 재업로드
- 같은 시작일/종료일의 `sales_stats` 자료가 이미 있고 파일이 다르면 경고한다.
- `기존 기간 자료를 새 파일로 교체` 체크 후에만 새 파일로 교체한다.
- 완전히 동일한 파일 해시는 기존 중복처리 규칙을 유지한다.

## 구현
- 모듈: `sales_period_v087.py`
- Streamlit 자료종류 선택을 감지해 해당 화면의 날짜 기본값만 변경한다.
- `core.import_sales_stats`를 감싸 동일기간 교체확인을 강제한다.
