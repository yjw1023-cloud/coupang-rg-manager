# 매입이력 메뉴 — v0.9.4

## 목적
상품별로 과거에 언제, 몇 개를, 얼마에 매입했는지 즉시 확인하기 위한 전용 메뉴다.
재고관리의 현재고와 역할을 분리하여 과거 매입 분석은 `🗂️ 매입이력` 메뉴에서 담당한다.

## 데이터 원칙
- 별도 매입이력 DB를 만들지 않는다.
- 기존 `purchase_lines.product_id`와 `products.id`를 연결해 조회한다.
- 기존 Claude ERP에서 이관된 과거 매입과 이후 매입 Excel로 등록되는 신규 매입이 같은 상품 이력에 연속 표시된다.
- 매입일: `purchase_lines.purchase_date`
- 차수: `purchase_lines.purchase_batch`
- 수량: `qty_receipt` 우선, 없으면 `qty_source`
- 개당 매입가: `landed_unit_cost_krw` 우선. 없으면 실제 총매입금액/수량, 마지막으로 `unit_price`를 사용한다.
- 매입금액: `landed_total_krw` 우선, 없으면 `total_amount`를 사용한다.
- 쿠팡 RG 내부 `CP-옵션ID` 코드는 사용자 화면에서 `CP-`를 숨긴다.

## v0.9.4 상품 선택 UX
기존 v0.9.2의 상품 선택 드롭다운은 사용하지 않는다.

`상품별 매입이력` 탭을 열면 매입이력이 있는 상품을 표 형태로 모두 나열한다.
기본 목록 컬럼:
- 품목코드
- 상품명
- 최근매입일
- 최근매입가
- 누적매입수량
- 누적매입액
- 매입횟수

사용자가 상품 행을 클릭하면 같은 화면 아래쪽에 해당 상품의 과거 매입이력을 표시한다.
상품 검색창은 선택용이 아니라 목록을 빠르게 좁히는 필터 용도다.

Streamlit 버전이 행 선택 기능을 지원하면 표의 행 클릭으로 선택한다.
행 선택 기능을 지원하지 않는 구버전에서는 전체 목록을 그대로 유지하면서 `선택` 체크 칸 방식으로 동작한다.

## 선택 상품 상세
상품을 클릭하면 다음 KPI를 표시한다.
- 최근 매입가
- 누적 매입수량
- 누적 매입액
- 최근 매입일
- 총 매입 건수 / 최근 차수

가장 먼저 보이는 상세 매입표는 필요한 핵심 정보만 표시한다.
- 매입일
- 차수
- 매입수량
- 개당 매입가
- 매입금액

매입이력이 2건 이상이면 날짜별 매입단가 추이를 차트로 표시한다.
원본 확인이 필요할 때는 접힌 영역에서 매입자료 상품명, 상세정보, 파일명, 시트, 원본행을 볼 수 있다.

## 전체 매입내역 화면
- 전체 매입 건수
- 매입상품 수
- 누적 매입액
- 차수별 필터
- 최신순 전체 매입표

## 관련 코드
- `purchase_history_v092.py`: 기본 매입이력 조회/계산
- `purchase_history_v094.py`: 상품목록 클릭 UX
- `app.py`
- `VERSION.txt` = `0.9.4`
- `update/latest.json` = `0.9.4`
