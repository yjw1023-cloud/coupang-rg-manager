# 매입자료 Excel 입력 / 상품 자동매칭

## v0.8 기준
- 메뉴: `🧾 매입관리`
- 매입 Excel에서 상품명은 기존 `품명(品名)` 열을 사용한다.
- **매입원가는 Excel W열을 고정으로 읽는다.** W열 값은 이미 원화(KRW) 기준의 1개당 매입원가로 취급한다.
- **매입수량은 Excel AB열을 고정으로 읽는다.** 기본 실제 입고수량도 AB열 수량으로 설정하며 필요할 때만 사용자가 수정한다.
- 매입금액은 `W열 매입원가 × AB열 수량`으로 계산한다.
- 환율 입력은 사용하지 않는다. W열 원가가 그대로 원화 입고원가/관리원가의 기준이다.
- 파일 제목/시트의 날짜 문구에서 매입일을 가능한 경우 자동 인식한다.

## 자체창고 매칭 원칙
- **매입자료의 상품은 자체창고 관리상품하고만 매칭한다.**
- 쿠팡RG에만 존재하는 판매상품/옵션은 매입 매칭 후보에서 제외한다.
- 매입은 항상 `자체창고`로 입고한다. 사용자가 매입 화면에서 쿠팡RG를 입고창고로 선택하지 않는다.
- 자체창고에 들어온 상품은 이후 BOM/생산 처리를 거쳐 완제품이 되고, 그 다음 쿠팡RG로 입고되는 흐름이다.
- 따라서 `매입 -> 자체창고 -> 생산/BOM -> 쿠팡RG` 흐름을 건너뛰어 매입상품을 RG 상품에 직접 연결하지 않는다.
- 자체창고 상품 판정은 원재료/기초상품이거나 자체창고 재고이력이 있는 상품을 우선 대상으로 한다.
- 기존에 직접 확정한 매입 별칭도 대상 상품이 자체창고 후보일 때만 자동 적용한다.
- 애매하면 `확인 필요`, 후보가 없으면 `신규 기초상품으로 등록` 또는 `이 행은 입고하지 않음`을 선택한다.
- 한 번 확정한 `매입자료 상품명 + 상세정보 -> 자체창고 상품` 대응은 `purchase_aliases`에 기억한다.
- 같은 원본 파일은 파일 해시로 중복 입고를 방지한다.

## 2026-07-30 실제 파일 검증
- 실제 업로드 파일의 W열/AB열 구조 확인 완료.
- 목운동 하네스: W 3,893원 / AB 30개
- 글러브 길들이기 밴드: W 998원 / AB 200개
- 석쇠 집게: W 434원 / AB 200개
- 검정가격표: W 214원 / AB 800개
- 반짇고리세트: W 3,862원 / AB 50개
- 상품행 8개 기준 매입금액 합계 1,346,790원으로 파싱 검증.

## 과거 v0.6과의 차이
- v0.6은 RMB 단가/총금액과 환율을 이용했으나 v0.8부터 해당 방식은 매입 입력 기준으로 사용하지 않는다.
- v0.6은 전체 시스템 상품을 매칭 후보로 사용했으나 v0.8 매입 화면은 자체창고 후보만 사용한다.
- 기존 ERP 이관 기능의 상품 매칭 규칙은 별도로 유지해 v0.8 매입 화면 필터가 이관 작업에 영향을 주지 않도록 한다.
