# 생산자료 Excel 일괄생산 — v0.9.5

## 목적
앞으로 쿠팡 로켓그로스 입고요청 Excel을 생산 입력 양식으로 사용한다.
사용자가 Excel에서 생산할 상품의 수량을 입력한 뒤 ERP에 업로드하면, ERP가 옵션ID로 완제품을 연결하고 BOM을 기준으로 자체창고 구성품 차감 → 쿠팡RG 완제품 입고를 처리한다.

## 입력 양식
기준 시트: `로켓그로스 입고`

사용 열:
- B열: 등록상품명 — 화면 확인용
- C열: 옵션명 — 화면 확인용
- G열: `옵션 ID` — ERP 상품 연결키
- V열: `입고 수량 입력(필수)` — ERP 생산수량

규칙:
- V열이 빈 행은 생산 대상에서 제외한다.
- V열에 1~5,000의 정수가 입력된 행만 생산 대상으로 읽는다.
- 상품명은 변경될 수 있으므로 매칭키로 사용하지 않고 옵션ID를 우선 사용한다.
- 같은 파일에 같은 옵션ID가 생산수량과 함께 두 번 나오면 중복생산 위험 때문에 전체 생산을 차단한다.

## 전 품목 사전검사 — 가장 중요한 규칙
Excel 업로드만으로 생산하지 않는다.

생산 실행 전 파일의 모든 생산 대상 행을 먼저 검사한다.
- 옵션ID 존재 여부
- ERP 상품 등록 여부
- 생산수량 유효성
- BOM 존재 여부
- self-BOM 오류 여부

**생산 대상 중 BOM이 없는 상품이 하나라도 있으면 전체 생산을 중단한다.**
- BOM이 정상인 다른 상품도 생산하지 않는다.
- 재고 차감 0건
- 완제품 입고 0건
- 생산이력 0건
- 화면에 BOM이 없는 상품 목록을 표시한다.
- 사용자에게 `BOM을 등록한 뒤 생산자료를 다시 업로드`하라고 안내한다.

옵션ID 미등록, 수량 오류, 중복 옵션ID 등 다른 차단 오류가 하나라도 있어도 동일하게 전체 생산을 중단한다.

## 실제 생산 실행
모든 행의 검증이 정상일 때만 `전체 생산 실행` 버튼을 사용할 수 있다.
버튼 실행 시 한 SQLite 트랜잭션에서 전체 상품을 처리한다.

상품별 처리:
1. BOM 구성품 필요수량 = `qty_per × 생산수량`
2. 구성품을 `자체창고`에서 `생산소모`로 차감
3. 완제품을 `쿠팡RG`에 `생산RG입고`로 입고
4. 완제품 생산원가를 BOM 구성품 원가 합계로 갱신
5. `production_orders`에 생산이력 기록

현재 ERP 원칙 유지:
- 구성품 재고가 부족해도 실제 생산은 기록한다.
- 부족분은 자체창고 마이너스 재고로 남긴다.
- self-BOM은 생산 차단.

전체 작업은 하나의 트랜잭션이므로 실행 중 오류가 발생하면 앞에서 처리한 상품까지 모두 롤백한다.

## 중복 생산 방지
성공적으로 생산한 Excel의 SHA-256 파일 해시를 `production_batch_imports.file_hash`에 UNIQUE로 저장한다.
같은 파일을 다시 올려 `전체 생산 실행`을 눌러도 두 번째 생산은 차단한다.

감사용 테이블:
- `production_batch_imports`
- `production_batch_lines`

이 테이블은 생산 Excel 배치의 파일명, 생산일, 전체 수량, 옵션ID, 상품, 수량, 생산원가, ref_no를 추적한다.

## UI
새 메뉴: `🏭 생산자료`

화면 흐름:
1. Excel 업로드
2. 생산일 선택
3. 생산대상 / 총 생산수량 / BOM 정상 / BOM 없음 KPI
4. 대상상품 검토표
5. 오류가 하나라도 있으면 전체 중단 메시지
6. 전부 정상이면 확인 체크
7. `전체 생산 실행`

파일 업로드 및 미리보기 단계에서는 재고와 생산이력을 변경하지 않는다.

## 2026-08-10 양식 검증
첨부된 `A00577001_20260805.xlsx`로 파서 형식만 검증했다.
- V열에 생산수량이 실제 입력된 행: 8개
- 수량 합계: 190개
- G열 옵션ID와 V열 수량을 정상 인식함.

**이 파일은 사용자의 지시에 따라 실제 생산 실행하지 않았다.**
DB의 재고/생산이력에는 이 파일을 반영하지 않았다.

## 관련 코드
- `production_batch_v095.py`
- `production_v085.py`
- `inventory_flow_v088.py`
- `app.py`
- `VERSION.txt` = `0.9.5`
- `update/latest.json` = `0.9.5`
