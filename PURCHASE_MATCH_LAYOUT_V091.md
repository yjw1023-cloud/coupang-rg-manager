# 매입 매칭표 레이아웃 — v0.9.1

사용자 요구에 따라 매입 Excel 업로드 후 매칭상품 정보를 한 셀에 이어 붙이지 않는다.

## 표시 순서
`No. | 매입상품 | 품목코드 | 상품명 | 현재재고 | 매입수량 | 매입원가 | 상태 | 매칭 수정`

## 규칙
- 기존 `상품명 | 품목코드 | 자체재고` 식의 합쳐진 문자열 표시는 사용하지 않는다.
- 매칭된 자체창고 상품의 `품목코드`, `상품명`, `현재재고`를 각각 독립된 표 칸으로 표시한다.
- 품목코드가 상품명보다 먼저 나온다.
- 잘못된 매칭은 맨 오른쪽 `매칭 수정` 셀에서 다른 자체창고 상품을 선택해 변경할 수 있다.
- 사용자가 매칭을 바꾸면 품목코드/상품명/현재재고 칸이 새 선택 기준으로 갱신된다.
- 기존 자동매칭 알고리즘과 매입확정 로직은 변경하지 않는다.
